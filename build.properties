src.dir=src
test.dir=src/test
lib.dir=lib
build.dir=build
classes.dir=${build.dir}/classes
jar.dir=${build.dir}/jar
reports.dir=reports
junit.dir=${reports.dir}/junit
cobertura.dir=${reports.dir}/cobertura
instrumented.dir=instrumented

cobertura.main.dir=/Users/heary/Documents/cobertura-2.1.1

junit.xml.dir=${reports.dir}/${junit.dir}/junit-xml
junit.html.dir=${reports.dir}/${junit.dir}/junit-html
coverage.xml.dir=${reports.dir}/${cobertura.dir}/cobertura-xml
coverage.summaryxml.dir=${reports.dir}/${cobertura.dir}/cobertura-summary-xml
coverage.html.dir=${reports.dir}/${cobertura.dir}/cobertura-html

